export type EventType = {
    id?: string,
    title: string,
    date_time: string,
    type: string,
    address: string,
    description: string,
    host: string,
    online: boolean,
    free: boolean,
    price: string,
    thumbnail: string,
    banner: string
}
